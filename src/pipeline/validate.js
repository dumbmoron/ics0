export function validateHeader(header) {
    return { error: null, value: header };
}

export function validateHeaderAndEvent(obj) {
    return { error: null, value: obj };
}

export const urlRegex = /.*/g;
